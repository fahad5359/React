import React from 'react'
// add style to iddintyfy the  component,then do some addtion ,finally import it to app component
export default function addition() {
    let a=10;
    let b=30
  return (
    <div className="stl">Again we made an addition that calculates a and b
    <p>10+30={a+b}</p>
    
     </div>
  )
}
